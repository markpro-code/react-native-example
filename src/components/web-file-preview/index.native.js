import React from 'react';
import { Text } from 'react-native';

export default function WebFilePreview() {
  return <Text style={{ color: 'red', fontSize: 24 }}>WebFilePreview not support in native</Text>;
}
