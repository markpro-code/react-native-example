import React from 'react';
import { Text } from 'react-native';
import { noop } from 'lodash';

export default function WebInfiniteCalendar() {
  return <Text style={{ color: 'red', fontSize: 14 }}>WebInfiniteCalendar: Not Implemented In Native</Text>;
}

export const withRange = noop;
export const Calendar = noop;
